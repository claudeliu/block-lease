import { DemoShell } from "@/components/demo-shell";

export const metadata = {
  title: "Demo",
  description: "Interactive Block Lease demo workspace."
};

export default function DemoPage() {
  return <DemoShell />;
}
